var kthSmallest = function (root, k) {
  if (!root) return root;

  let arr = [];

  function back(root) {
    if (root === null) {
      return;
    }

    back(root.left);
    arr.push(root.val);
    back(root.right);
  }
  back(root);

  return arr[k - 1];
};
